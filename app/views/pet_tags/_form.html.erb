<%= form_for(pet_tag) do |f| %>
  <% if pet_tag.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(pet_tag.errors.count, "error") %> prohibited this pet_tag from being saved:</h2>

      <ul>
      <% pet_tag.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :id_pet %>
    <%= f.number_field :id_pet %>
  </div>

  <div class="field">
    <%= f.label :id_tag %>
    <%= f.number_field :id_tag %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
